# アーキテクチャドキュメント

のうかつ - 3世代つながる脳トレアプリ

---

## 技術スタック

| カテゴリ | 技術 | バージョン | 用途 |
|---------|------|-----------|------|
| フレームワーク | Next.js | 16.1.6 | App Router, SSR, ルーティング |
| 言語 | TypeScript | 5.x | 型安全な開発 |
| UI ライブラリ | React | 19.2.3 | コンポーネントベースUI |
| スタイリング | Tailwind CSS | 4.x | ユーティリティファーストCSS |
| 状態管理 | Zustand | 5.0.11 | 軽量グローバルステート |
| アニメーション | motion/react | 12.34.0 | UI アニメーション・トランジション |
| アイコン | Lucide React | 0.564.0 | SVGアイコンライブラリ |
| テスト | Vitest | 4.0.18 | ユニットテスト |
| テストユーティリティ | Testing Library | 16.3.2 | React コンポーネントテスト |
| CSS ユーティリティ | clsx + tailwind-merge | - | 条件付きクラス名結合 |

---

## ディレクトリ構造

```
app/src/
├── app/                          # Next.js App Router
│   ├── globals.css               # グローバルCSS・デザイントークン
│   ├── layout.tsx                # ルートレイアウト（フォント設定、メタデータ）
│   ├── favicon.ico               # ファビコン
│   └── (main)/                   # メインルートグループ
│       ├── layout.tsx            # メインレイアウト（ボトムナビゲーション）
│       ├── page.tsx              # ホーム画面
│       ├── challenge/
│       │   └── page.tsx          # チャレンジ（脳トレ問題）画面
│       ├── family/
│       │   └── page.tsx          # ファミリー画面
│       ├── report/
│       │   └── page.tsx          # レポート・記録画面
│       ├── settings/
│       │   └── page.tsx          # 設定画面
│       └── character/
│           └── page.tsx          # キャラクター・きせかえ画面
│
├── components/                   # 共有コンポーネント
│   └── game/
│       ├── QuestionCard.tsx      # 問題カード（選択肢UI・フィードバック）
│       ├── TimerBar.tsx          # タイマーバー（残り時間表示）
│       └── ResultScreen.tsx      # 結果画面（スコア表示・紙吹雪）
│
├── lib/                          # ユーティリティ・ロジック
│   ├── utils.ts                  # 汎用ユーティリティ（cn, 日付, 統計, ID生成）
│   └── game-engine/              # ゲームエンジン
│       ├── difficulty.ts         # 難易度調整アルゴリズム
│       ├── scoring.ts            # スコア計算ロジック
│       └── questions.ts          # 問題生成（カードマッチ、単語記憶、ストループ、暗算、アナグラム）
│
├── stores/                       # Zustand ストア
│   ├── user.ts                   # ユーザー・設定ストア
│   ├── game.ts                   # ゲーム状態ストア
│   ├── score.ts                  # スコア・記録ストア
│   ├── family.ts                 # ファミリーストア
│   └── character.ts              # キャラクター・きせかえストア
│
├── types/                        # 型定義
│   └── index.ts                  # 全型定義・定数
│
└── __tests__/                    # テストファイル
    ├── utils.test.ts             # utilsのテスト
    ├── difficulty.test.ts        # 難易度調整のテスト
    ├── scoring.test.ts           # スコア計算のテスト
    └── questions.test.ts         # 問題生成のテスト
```

---

## データフロー

### ストア間の関係

```
useUserStore ─────────────────┐
  │ user, settings, uiMode    │
  │                            │
  ├──→ HomePage (挨拶、UIモード)
  ├──→ MainLayout (UIモード → data-mode属性)
  ├──→ SettingsPage (設定変更)
  └──→ FamilyPage (送信者情報)

useGameStore ─────────────────┐
  │ challenge, phase, answers  │
  │                            │
  ├──→ ChallengePage (ゲーム実行フロー)
  ├──→ QuestionCard (現在の問題表示)
  ├──→ TimerBar (残り時間)
  └──→ ResultScreen (結果表示)
            │
            ↓ addScore()
useScoreStore ────────────────┐
  │ scores, dailyScores,       │
  │ categoryScores, streak     │
  │                            │
  ├──→ HomePage (今日のスコア、カテゴリスコア)
  ├──→ ReportPage (統計、グラフ、カレンダー)
  └──→ CharacterPage (感情決定)

useFamilyStore ───────────────┐
  │ family, members, messages  │
  │                            │
  ├──→ HomePage (ファミリーサマリー)
  └──→ FamilyPage (メンバー一覧、ランキング、スタンプ)

useCharacterStore ────────────┐
  │ characterState, items      │
  │                            │
  ├──→ HomePage (キャラクター状態)
  └──→ CharacterPage (きせかえショップ)
```

### ゲームフロー

```
カテゴリ選択 → ゲーム開始 → 問題表示 → 回答 → 次の問題 → ... → 結果画面 → スコア保存
    │              │           │         │                         │
    │         startGame()  getCurrentQuestion()  answerQuestion()  addScore()
    │              │           │         │                         │
    └── gamePhase: idle → ready → playing ←→ paused → result ────┘
```

---

## 状態管理設計

### 1. useUserStore（ユーザーストア）

| State | 型 | 説明 |
|-------|-----|------|
| `user` | `User \| null` | 現在のユーザー情報 |
| `settings` | `UserSettings` | UIモード、フォントサイズ、テーマ等 |
| `isOnboarded` | `boolean` | オンボーディング完了フラグ |

**Actions**: `setUser`, `updateSettings`, `setUIMode`, `setOnboarded`, `logout`

**永続化**: `localStorage` (key: `brain-user-store`)

### 2. useGameStore（ゲームストア）

| State | 型 | 説明 |
|-------|-----|------|
| `currentChallenge` | `Challenge \| null` | 実行中のチャレンジ |
| `currentQuestionIndex` | `number` | 現在の問題番号 |
| `answers` | `Answer[]` | 回答履歴 |
| `gamePhase` | `GamePhase` | idle/ready/memorize/playing/paused/result |
| `timeRemaining` | `number` | 残り時間（秒） |
| `isTimerRunning` | `boolean` | タイマー実行中フラグ |

**Actions**: `startGame`, `setGamePhase`, `answerQuestion`, `nextQuestion`, `pauseGame`, `resumeGame`, `finishGame`, `resetGame`, `setTimeRemaining`, `tick`

**Computed**: `getCurrentQuestion`, `getProgress`, `isLastQuestion`

**永続化**: `localStorage` (key: `brain-game-store`)

### 3. useScoreStore（スコアストア）

| State | 型 | 説明 |
|-------|-----|------|
| `scores` | `Score[]` | 全スコア履歴 |
| `dailyScores` | `DailyScore[]` | 日別集計スコア |
| `categoryScores` | `CategoryScore[]` | カテゴリ別最新スコア |
| `streakCount` | `number` | 現在の連続日数 |
| `bestStreak` | `number` | 最高連続日数 |

**Actions**: `addScore`, `setScores`, `setDailyScores`, `setCategoryScores`, `updateStreak`

**Computed**: `getDailyScores`, `getCategoryTrend`, `getWeeklyAverage`, `getLatestScore`, `getScoresByCategory`, `getTotalScoreForDate`

**永続化**: `localStorage` (key: `brain-score-store`)

### 4. useFamilyStore（ファミリーストア）

| State | 型 | 説明 |
|-------|-----|------|
| `family` | `Family \| null` | 参加中のファミリー |
| `members` | `FamilyMember[]` | メンバー一覧 |
| `messages` | `Message[]` | メッセージ履歴 |
| `unreadCount` | `number` | 未読メッセージ数 |

**Actions**: `setFamily`, `clearFamily`, `setMembers`, `updateMember`, `addMessage`, `setMessages`, `markRead`, `markAllRead`

**Computed**: `getUnreadCount`, `getMember`, `getRecentMessages`

**永続化**: `localStorage` (key: `brain-family-store`)

### 5. useCharacterStore（キャラクターストア）

| State | 型 | 説明 |
|-------|-----|------|
| `characterState` | `CharacterState` | バリアント、感情、レベル、コイン、装備 |
| `availableItems` | `CharacterItem[]` | 購入可能アイテム一覧 |

**Actions**: `addCoins`, `spendCoins`, `equipItem`, `unequipItem`, `buyItem`, `setVariant`, `setEmotion`, `setLevel`, `setAvailableItems`

**Computed**: `getEmotion`, `canAfford`, `getOwnedItems`, `getEquippedItems`

**永続化**: `localStorage` (key: `brain-character-store`)

---

## ルーティング

Next.js App Router を使用。`(main)` ルートグループ配下に全画面を配置。

| パス | ファイル | 画面名 | 説明 |
|------|---------|--------|------|
| `/` | `(main)/page.tsx` | ホーム | キャラクター挨拶、今日のトレーニングボタン、カテゴリスコア、ファミリーサマリー |
| `/challenge` | `(main)/challenge/page.tsx` | チャレンジ | カテゴリ選択、おまかせトレーニング、問題出題、結果表示 |
| `/family` | `(main)/family/page.tsx` | かぞく | ファミリー参加/作成、メンバー一覧、週間ランキング、スタンプ送信 |
| `/report` | `(main)/report/page.tsx` | きろく | 統計サマリー、スコア推移グラフ(SVG)、カテゴリ別バーグラフ、カレンダー |
| `/settings` | `(main)/settings/page.tsx` | せってい | UIモード、テーマ、フォントサイズ、サウンド、通知、言語、アカウント |
| `/character` | `(main)/character/page.tsx` | キャラ | キャラクター表示、台詞、レベル、コイン、きせかえショップ |

### レイアウト構造

```
RootLayout (app/layout.tsx)
  └─ フォント設定 (Zen Maru Gothic, Noto Sans JP)
  └─ メタデータ (title, description, viewport)
      │
      └─ MainLayout (app/(main)/layout.tsx)
          └─ data-mode属性 (UIモードに応じてフォントサイズ変更)
          └─ ボトムナビゲーション (5タブ: ホーム, かぞく, キャラ, きろく, せってい)
          └─ lg以上ではナビゲーション非表示
```

---

## デザインシステム

### CSS変数 (globals.css)

デザイントークンはCSS変数で定義し、ライトテーマ/ダークテーマを切り替え可能。

#### カラーパレット

| 変数名 | ライト | ダーク | 用途 |
|--------|--------|--------|------|
| `--primary` | `#3D8B5F` | `#5BA97D` | メインカラー（緑系） |
| `--secondary` | `#E8845C` | `#F0A080` | サブカラー（オレンジ系） |
| `--accent` | `#F2C94C` | `#F5D76E` | アクセント（黄色系） |
| `--background` | `#FBF8F3` | `#1A2332` | 背景色 |
| `--surface` | `#FFFFFF` | `#243040` | カード・パネル背景 |
| `--foreground` | `#2C3E2D` | `#E8E4DE` | テキスト色 |
| `--success` | `#5BA97D` | `#7BC09A` | 成功 |
| `--warning` | `#E8845C` | `#F0A080` | 警告（赤色禁止: コーラル/オレンジ系） |
| `--error` | `#D06A42` | `#F0A080` | エラー（赤色禁止: コーラル系） |
| `--info` | `#5B9BD5` | `#7BB5E0` | 情報 |

**設計原則**: 高齢者向け認知症予防アプリのため、赤色(`#FF0000`系)は使用禁止。警告・エラーにはコーラル/オレンジ系を使用。

#### 世代別アクセントカラー

| 変数名 | カラー | 対象世代 |
|--------|--------|---------|
| `--generation-senior` | `#4AA3A2` | シニア（祖父母） |
| `--generation-adult` | `#5B9BD5` | 大人（親世代） |
| `--generation-child` | `#E88CA5` | 子ども |

#### タイポグラフィ

| 変数名 | フォント | 用途 |
|--------|---------|------|
| `--font-heading` | Zen Maru Gothic, Nunito | 見出し・タイトル |
| `--font-body` | Noto Sans JP, Nunito Sans | 本文 |
| `--font-ui` | Noto Sans JP, Nunito Sans | UIラベル |

#### 角丸・影

| 変数名 | 値 | 用途 |
|--------|-----|------|
| `--radius-sm` | 8px | 小要素 |
| `--radius-md` | 12px | 中要素 |
| `--radius-lg` | 16px | カード |
| `--radius-xl` | 20px | 大型パネル |
| `--radius-full` | 9999px | 丸型ボタン |
| `--shadow-sm` | 小さい影 | カード |
| `--shadow-md` | 中程度の影 | ホバー |
| `--shadow-lg` | 大きい影 | CTA ボタン |

### UIモード

`data-mode` 属性で3つのUIモードを切り替え可能。

| モード | フォントサイズ | 行間 | 対象ユーザー |
|--------|-------------|------|------------|
| `senior` | 20px | 1.8 | 高齢者（大きい文字、シンプル操作） |
| `standard` | デフォルト | デフォルト | 一般ユーザー |
| `kids` | 18px | 1.7 | 子ども（楽しい見た目） |

### テーマ切り替え

- **system**: OSのダークモード設定に追従
- **light**: ライトテーマ固定
- **dark**: ダークテーマ固定 (`.dark` クラスで切り替え)

### アクセシビリティ

- `prefers-reduced-motion: reduce` 対応 (motion/react の `useReducedMotion` フック使用)
- 全インタラクティブ要素のタッチターゲット: 最小44px
- `aria-label`, `aria-expanded`, `aria-pressed`, `aria-current`, `role` 属性の適切な使用
- `tabular-nums` によるスコア表示の数字幅統一
- `text-balance` によるテキストバランス

### 認知機能カテゴリ

| カテゴリ | 日本語名 | カラー | 絵文字 | 問題タイプ |
|---------|---------|--------|--------|----------|
| memory | 記憶力 | `#3D8B5F` | 🧠 | カードマッチ, 単語記憶 |
| attention | 注意力 | `#E8845C` | 👁 | ストループテスト |
| language | 言語力 | `#5B9BD5` | 💬 | アナグラム |
| math | 計算力 | `#F2C94C` | 🔢 | 暗算 |
| spatial | 空間認識 | `#9B72CF` | 📦 | (未実装、暗算で代替) |

---

## 認知症予防の設計方針

1. **ポジティブフィードバック**: 不正解でも否定的な表現を避け、「おしい！つぎはできるよ！」「チャレンジしてえらい！」など励ましの言葉を使用
2. **赤色不使用**: 不安感を与えないよう、エラー・警告にはコーラル/オレンジ系を使用
3. **3世代対応**: UIモード切り替えにより、高齢者・大人・子どもそれぞれに最適な表示を提供
4. **家族のつながり**: スタンプ機能・週間ランキングで離れた家族間のコミュニケーションを促進
5. **継続動機づけ**: 連続日数(ストリーク)、キャラクター育成、きせかえショップによるゲーミフィケーション

---

*のうかつ v0.1.0 - アーキテクチャドキュメント*
